## Codebase Context

<!-- What existing files, modules, and areas of the codebase are relevant to this change?
     Read the proposal and specs first, then locate actual files. Cite paths.
     Confidence: Confident | Likely | Unclear -->

## Patterns and Conventions

<!-- How does this codebase handle concerns similar to this change?
     (state management, API calls, error handling, testing patterns, etc.)
     Reference real files — not assumptions. -->

## Integration Points

<!-- What existing code will the new code hook into?
     Identify entry points, data flows, and seams.
     Flag any integration that looks risky or requires changes to existing APIs. -->

## Reusable Assets

<!-- What already exists that can be leveraged?
     (utilities, components, hooks, helpers, shared types)
     Cite file paths. The design and task authors should use these instead of re-implementing. -->

## Risks and Unknowns

<!-- What could make this harder than expected?
     Format each item as:
     - [Risk/Unknown] → Confidence: Confident | Likely | Unclear
     "Unclear" items are decisions that the design doc must resolve before tasks are written. -->

## Ecosystem Research

<!-- Only fill in when a trigger condition applies (see openspec-research skill):
     (a) greenfield work — little or no relevant existing codebase
     (b) new library or technology being introduced
     (c) tech-choice decision between approaches
     Skip this section entirely for brownfield changes where codebase patterns are clear.

     For each finding, cite the source URL. Confidence model:
     - Confident: official documentation (library's own docs site)
     - Likely: multiple independent credible sources aligned
     - Unclear: single blog post, conflicting sources, or no clear consensus

     Surface "don't hand-roll" solutions — well-adopted libraries that solve the core problem. -->
