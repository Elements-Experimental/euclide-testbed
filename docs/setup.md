# Setup

This file is generated by [euclide](https://github.com/Elements-Experimental/euclide) and updated on `copier update`.

## Template prompts

| Prompt | Default | Description |
|---|---|---|
| `project_name` | _(required)_ | Name of the project |
| `tech_stack` | _(required)_ | Primary tech stack (e.g. "Python + FastAPI", "Node + TypeScript") |
| `default_branch` | `dev` | Default integration branch name (`main` is reserved for releases) |
| `metrics_enabled` | `true` | Enable merge metrics GitHub Actions workflow |
| `metrics_sheet_name` | `Sheet1` | Google Sheet tab name that receives metric rows (only when `metrics_enabled` is true) |

## Post-bootstrap checklist

Complete after `copier copy --trust`:

- [ ] Create and push the `dev` branch; set it as default in GitHub Settings → General
- [ ] Add branch protection on `dev`: require PR reviews, no direct pushes
- [ ] Authenticate the `gh` CLI: `gh auth login`
- [ ] Allow Actions to open PRs: **Settings → Actions → General → Workflow permissions** → select "Read and write permissions" and check "Allow GitHub Actions to create and approve pull requests"
- [ ] Add CI: create `.github/workflows/ci.yml` for your stack and add its job names as required status checks on `dev`
- [ ] Fill in `README.md`: add a short project description and quickstart/run instructions
- [ ] Update `docs/architecture.md` with project architecture details
- [ ] Update `docs/testing.md` with lint, test, and build commands

- [ ] Configure Google Sheet for metrics: follow the instructions in `docs/metrics.md`
- [ ] Add `METRICS_SHEET_ID` secret — Google Sheet ID (from the sheet URL)
- [ ] Add `METRICS_SA_KEY` secret — base64-encoded service account JSON key
- [ ] Confirm the sheet tab is named `Sheet1` (rename the tab or re-run `copier update` with a different value if needed)


## Updating

Pull in upstream template changes at any time:

```bash
copier update --trust
```

To pull from a specific branch or tag instead of the default:

```bash
copier update --trust --vcs-ref <branch-or-tag>
```

For example, to pin to a release: `copier update --trust --vcs-ref v0.5.0`.
